import {LanguageSwitcherV2Presenter} from "./LanguageSwitcherV2Presenter";
import usePopover from "../../hooks/popover/usePopover";

export const LanguageSwitcherV2: React.FC<{  }> = props => {

    return (
        <LanguageSwitcherV2Presenter

        />
    );
};